"""
Verification Layer — core/verification package.

Provides deterministic, non-LLM checks to ensure system integrity:
  - Static Analysis: Syntax checking, type checking (mypy), linting (ruff).
  - Architecture Validator: Ensures code doesn't violate Knowledge Graph constraints.
"""
